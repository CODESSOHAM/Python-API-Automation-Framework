[behave]
paths = features
default_format = pretty
show_timings = true
junit = false

# Run with the Allure formatter separately (see README), e.g.:
# behave -f allure_behave.formatter:AllureFormatter -o reports/allure-results
